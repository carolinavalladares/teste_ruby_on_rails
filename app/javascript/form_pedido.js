document.addEventListener("DOMContentLoaded", () => {
  console.log("loaded");

  document.querySelector("#add_item").addEventListener("click", (e) => {
    // const currentTime = new Date().getTime();
    // const template = document.querySelector("#item_fields_template");
    // const itemsContainer = document.querySelector("#pedido_items");
    // template.innerHTML = template.innerHTML.replace(
    //   /new_item/g,
    //   `new_${currentTime}`
    // );
    // // const item = document.createElement("div");
    // // item.classList.add(`item_${currentTime}`);
    // // item.innerHTML = template.innerHTML;
    // itemsContainer.insertAdjacentElement("beforeend", template);
  });
});
